sudo: required
language: perl6
perl6:
    - latest
cache:
    directories
    - $HOME/Net-IP-Lite-Perl6
install:
    - rakudobrew build-panda
    - git clone https://github.com/tbrowder/Net-IP-Lite-Perl6.git
    - panda install ./Net-IP-Lite-Perl6
    - panda installdeps .
